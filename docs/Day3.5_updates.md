# Обновление логики дня 3
Список ихменений:
1) Переделана функция авторизации **authenticate_client**. Теперь у банка в константе **AUTH_TRIES** прописано кол-во попыток аутентификации: с каждой неверной попыткой счетчик уменьшается, что ведет к заморозке клиента, которого пытаются авторизовать.
2) Поправлен баг с неправильным подсчетом суммы на счетах клиента в **get_total_balance**.
3) Исправлен баг, из-за которого не проставлялся клиенту флаг подозрительной активности.
4) Изменена структура проекта. Теперь выделен отдельный пакет bank с модулями init, bank и errors. Появилась возможность писать независимые тесты отдельными файлами в папке tests/...

